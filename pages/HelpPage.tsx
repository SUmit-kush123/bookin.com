import React from 'react';

const HelpPage: React.FC = () => {
  // This page was likely replaced by CustomerSupportPage.
  // This file was empty, which can cause runtime errors.
  // Providing a valid, empty component to prevent these issues.
  // If you see this, it probably means an old link is pointing to a /help route that no longer exists.
  return null;
};

export default HelpPage;
